//! The service as a library, so every layer is reachable from its tests and from the entry point alike.
//!
//! The modules this project was generated with are declared below. A layer that arrives later — the
//! domain's first model, the application's first use case — is one more `pub mod` line.

__MODULES__
